export function LogoFull({ className = 'h-9' }) {
  return (
    <img
      src="/logo-full.png"
      alt="Prepify"
      className={`object-contain ${className}`}
    />
  )
}

export function LogoIcon({ className = 'h-8' }) {
  return (
    <img
      src="/logo-icon.png"
      alt="Prepify"
      className={`object-contain ${className}`}
    />
  )
}

export function LogoText({ className = 'h-8' }) {
  return (
    <img
      src="/logo-text.png"
      alt="Prepify"
      className={`object-contain ${className}`}
    />
  )
}